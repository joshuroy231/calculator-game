#include <stdbool.h>

#include "components.hpp"

class Event {
    public:
    virtual bool isTrue() = 0;
};

class Event {
    private:
        virtual bool condition() const = 0;
}

struct Actor {
    Position* position;
    Dimensions* dimensions;
    Color* color;
};

class Scene {
    private:
    public: 
};